# Copiar a `.env` y rellenar.
#
# Solo estas tres variables llegan al navegador. Todo lo que lleve el prefijo
# VITE_ acaba en el bundle: no pongas aquí ni la clave de DeepL ni la
# service_role. Esas dos viven exclusivamente en los secrets de Supabase.

VITE_SUPABASE_URL=https://xxxxxxxxxxxxxxxx.supabase.co

# Clave pública (anon / publishable). Es segura en el cliente: RLS es lo que
# protege los datos.
VITE_SUPABASE_ANON_KEY=

# Opcional. Si se deja vacío, la app usa la primera sala de la que el usuario
# sea miembro, que con una sola sala es siempre la correcta.
VITE_ROOM_ID=
